*** CREATE A PROJECT :  first_git_project
*** CREATE A GITHUB REPSITORY:
    *Pushingthe code FOR THE FIRST TIME
    git init -> create .git folder in the project. do once for each project
    git add . -> add all of teh code
    git commit -m "first commit"
    git branch -M main
    git remote add origin https://github.com/30215121/first_git_project.git
    git push -u origin main

    *** Pushingthe code FOR THE SECOND TIME

   git add .
   git commit -m "first commit"