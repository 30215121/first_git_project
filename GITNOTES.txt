*** CREATE A PROJECT :  first_git_project
*** CREATE A GITHUB REPSITORY:
    *Pushingthe code FOR THE FIRST TIME
    git init -> create .git folder in the project. do once for each project
    git add . -> add all of teh code
    git commit -m "first commit" -> creates a new version(commit)
    git branch -M main  -> creating a main branch
    git remote add origin https://github.com/30215121/first_git_project.git -> linking local repo and remote repo.
    git push -u origin main -> sending the code to that link

    *** Pushingthe code FOR THE SECOND TIME
    - add a new code : Notes2.txt
   git add .
   git commit -m "message"
   git push
-------------------------
*** GIT PULL
git PULL  is used to get the lastest code from github
git fetch or git merge  these are also do same thing with git pull
